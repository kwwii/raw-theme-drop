# raw-theme-drop
OpenSUSE desktop background and boot sequence source files

Leap and TUmbleweed have their own directories with svg files for the backgrounds
 - The boot sequence backgrounds are in their own directory "Boot"
